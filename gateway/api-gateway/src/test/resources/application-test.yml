spring:
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: "test-secret-key-for-gateway-test-must-be-at-least-256-bits-long!!"

gateway:
  blacklist-fail-policy: fail-close
  allowed-origins: http://localhost:3000

logging:
  level:
    root: WARN
    io.kyungseo: DEBUG
