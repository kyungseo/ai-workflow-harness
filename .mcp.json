{
  "mcpServers": {
    "postgres": {
      "command": "sh",
      "args": [
        "-c",
        "npx -y @modelcontextprotocol/server-postgres postgresql://${DB_USERNAME}:${DB_PASSWORD}@localhost:5432/${DB_NAME:-msa_db}"
      ]
    }
  }
}
