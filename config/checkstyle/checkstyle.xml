<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">
<!--
    Project: base-msa-template
    Base: Google Java Style Guide with project-specific overrides
    Overrides: LineLength=120, Indentation=4 (Google defaults: 100, 2)
    Javadoc: disabled — minimal comment policy (docs/coding-conventions.md)
    Suppressions: config/checkstyle/suppressions.xml
-->
<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <module name="SuppressionFilter">
        <property name="file" value="${config_loc}/suppressions.xml"/>
        <property name="optional" value="false"/>
    </module>

    <!-- File-level: no tab characters -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <!-- File-level: newline at end of file -->
    <module name="NewlineAtEndOfFile"/>

    <!-- Line length: 120 chars. import/URL lines excluded. -->
    <module name="LineLength">
        <property name="fileExtensions" value="java"/>
        <property name="max" value="120"/>
        <property name="ignorePattern" value="^package.*|^import.*|a href|href|http://|https://|ftp://"/>
    </module>

    <module name="TreeWalker">

        <!-- ================================================================
             Naming Conventions
             ================================================================ -->
        <module name="PackageName">
            <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
        </module>
        <module name="TypeName"/>
        <module name="MemberName">
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>
        <module name="ParameterName">
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>
        <module name="LocalVariableName">
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>
        <module name="CatchParameterName">
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>
        <module name="ConstantName"/>

        <!-- ================================================================
             Imports
             ================================================================ -->
        <module name="AvoidStarImport"/>
        <module name="RedundantImport"/>
        <module name="UnusedImports">
            <property name="processJavadoc" value="false"/>
        </module>

        <!-- ================================================================
             Class Structure
             ================================================================ -->
        <module name="OuterTypeFilename"/>
        <module name="OneTopLevelClass"/>
        <module name="NoFinalizer"/>

        <!-- ================================================================
             Control Flow: braces required for all blocks
             ================================================================ -->
        <module name="NeedBraces"/>
        <module name="EmptyCatchBlock">
            <!-- Allow empty catch only when variable is named 'expected' or 'ignore' -->
            <property name="exceptionVariableName" value="expected|ignore"/>
        </module>

        <!-- ================================================================
             Code Quality
             ================================================================ -->
        <module name="MultipleVariableDeclarations"/>
        <module name="FallThrough"/>
        <module name="UpperEll"/>

    </module>
</module>
