---
description: Repository execution, build, test, and verification commands.
globs:
alwaysApply: true
---

# Execution Rules

Default verification commands:

- `./gradlew test`
- `./gradlew build`

MUST:

- Prefer the Gradle wrapper over system Gradle.
- Choose the narrowest verification command that proves the change.
- Report any command you could not run and why.
- Follow the checkpoint flow in `docs/CLAUDE.md` before Docker, database, or end-to-end validation.

NEVER:

- Run destructive commands.
- Run privileged commands.
- Use alternate build tools unless the repository explicitly requires them.

Allowed checkpoint commands ONLY when required by `docs/CLAUDE.md`:

- `docker compose ...`
- `psql ...`
