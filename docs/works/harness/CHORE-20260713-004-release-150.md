---
id: CHORE-20260713-004
priority: P2
status: Active
risk: L2
scope: source develop→main 1.5.0 minor 릴리즈(readiness + release event 준비까지). 릴리즈 구성 = DR-043 framework-pure AGENT-WORKFLOW(6/24) + cross-review workflow 신설(command/skill/adapter/manual) + manifest contract 정비(CHORE-20260713-003). **R1-F3 반영: fleet upgrade는 이 Work에서 분리** — post-release follow-up 후보로 backlog 등록(spring·ai-deck·rfx tag 기준 + toolstead fresh-session canary). develop 직접 반영안은 기각(version-skew 재생산·DR-028 충돌) — 사용자 합의.
appetite: 0.5d
planned_start: 2026-07-13
planned_end: 2026-07-14
actual_end:
related_dr: [DR-028, DR-043]
related_work: [CHORE-20260713-003, CHORE-20260622-002]
---

# CHORE-20260713-004: 1.5.0 릴리즈 + fleet upgrade

## Top Summary

adopter fleet를 균일 baseline으로 올려 후속 upgrade/마이그를 수월하게 하자는 사용자 제안에서 출발. develop 직접 반영은 오늘 확정한 version-skew 감지 체계와 모순(same-version skew WARN 상시 점등)이고 DR-028(released tag baseline 기본)과 충돌하므로, **1.5.0 릴리즈 → clean tag 기준 fleet upgrade** 경로로 합의했다. rebaseline 과정에서 4개 adopter manifest가 전부 provenance 필드를 획득해 이후 skew가 구조적으로 관리된다(toolstead의 기존 skew도 해소).

## Scope

1. **Release prep:** VERSION 1.4.0→1.5.0(MINOR — 신규 workflow command + 하위호환 필드/parser 강화, DR-028 §2), release note(사용자 친화 + 호환성 항목 — -003 Discovery release handoff 소비), Release Full Sweep(deterministic 자동분 + judgment 점검), §3-1 Public Clean Baseline gate.
2. **Release:** develop→main PR(regular merge), tag `ai-workflow-v1.5.0`, gh release, post-merge develop sync.
3. **Cross-agent:** release readiness review(Codex — 1.4.0 선례 CHORE-20260622-002의 readiness 합의 패턴).
4. **Lifecycle (R1-F3):** release 전 Done Work 4건(-001~-004) archive로 §3-1 Public Clean Baseline(Active leakage 0, archive-pending 0) 충족. tag/gh release/develop sync는 Work close 후의 mechanical 실행 단계.

**비목표:** fleet upgrade 실행(post-release follow-up Work — backlog 등록), upgrade 절차 diet 착수, **신규 adopter 기준 incompatible/MAJOR contract 도입 없음**(기존 adopter에는 DR-043 one-time migration 등 호환성 주의 항목 존재 — release note 명시).

## Done Criteria

- [x] Release Full Sweep deterministic 자동분 + onboarding + secret/identity PASS (CP1). final state gate(§3-1)는 archive 정리 후 재실행 (CP2)
- [ ] VERSION 1.5.0 + release note — 호환성 4항목(python3 필수화, hash_mode canonical, provenance, **DR-043 one-time migration** + playbook pointer), "조치 불요"는 hash_mode/provenance에 한정, `/cross-review` manual-relay 경계 명시, 한 줄 요약·`검증` 섹션·compare link·Product backlog live-only 항목 포함 (R1-F1/F2)
- [ ] fleet upgrade follow-up이 backlog에 등록되고 이 Work 비범위로 분리 (R1-F3)
- [ ] Done Work 4건(-001~-004) archive + §3-1 final gate 통과 기록
- [ ] Codex readiness consensus(R1b — final gate evidence 확인) + 사용자 release-go 승인
- [ ] 사용자 최종 리뷰 (develop→main merge·tag·gh release·sync는 close 후 mechanical 실행)

## Verification

- `bash scripts/tests/run-harness-checks.sh --all`, `check-onboarding-flows.sh`, secret/private-info scan, stale identity audit, VERSION↔manifest 일관성(Layer R), tag 직후 clean-tag `--check` 3 adopter
- Surface: 전 출하 표면(release) + adopter cascade(fleet)

## Risk / Reversal Cost

- 릴리즈 자체는 선례 절차(1.4.0) 재사용, breaking 없음(MINOR). python3 필수화는 §호환성 주의로 명시(DR-028 §현 단계 정책 — MINOR라도 주의 표기). fleet upgrade는 agent-first 실증 2건 위에서 수행, 각 repo feature branch라 rollback 용이. **Reversal Cost: Medium**(tag/release는 되돌리기 비용 있음 — readiness gate로 완화).

## Discovery

- 착수: 2026-07-13. backlog row 없음(사용자 직접 지시 — 릴리즈는 backlog 후보가 아닌 이벤트성 Work, 1.4.0 선례 동일).

## Cross-Agent Review And Discussion

Model: manual relay. Driver = Claude, Reviewer = Codex, Arbiter = User. Rounds: readiness R1(+필요시 반복).

### R1 — Release Readiness Review (Cross-Agent Relay Packet)

**Role:** Driver = Claude / Reviewer = Codex / Arbiter = User

**Target:** develop @ `d4a772f` = 1.5.0 release candidate. 구성: 1.4.0 tag 이후 shipped delta(cross-review workflow 4표면 + docs/user 2 + AGENT-WORKFLOW DR-043 + create-harness.sh manifest contract) + VERSION 1.5.0 + release note 초안(CP1).

**Review Objective (release-go red-team):**
1. **release note 정확성:** 초안이 shipped delta를 과장/누락 없이 반영하는가. 특히 호환성 3항목(python3 필수화, hash_mode canonical, provenance)이 adopter 관점에서 충분·정확한가.
2. **half-implemented 표면 0 확인:** 1.4.0 이후 develop에 반쯤 걸친 표면이 없는가(1.4.0 readiness의 핵심 기준 재사용).
3. **MINOR 판정(DR-028 §2):** breaking 여부 — python3 필수화가 MINOR+호환성 주의로 충분한가, MAJOR 사유인가.
4. **sweep 결과 재현(선택):** `run-harness-checks.sh --all`, `check-onboarding-flows.sh`.
5. non-blocking이면 명시하고 release-go 조건부 가능.

**Do Not Re-litigate:** 릴리즈 자체의 필요성(사용자 합의 — fleet upgrade 선행 조건), 구성 요소 각각의 품질(각 Work에서 cross-review 완료: cross-review=CHORE-20260622-003, contract=-003 R0~R1c).

**Output Contract:** Verdict(release-go / conditional / hold) + finding을 이 파일 `### R1 — Reviewer Findings (Codex)` 섹션에 기록 + Round Log 갱신 + 5줄 요약 별도 출력.

### R1 — Reviewer Findings (Codex)

**Verdict: conditional**

`ai-workflow-v1.4.0..d4a772f`의 shipped surface와 실제 scaffold를 대조한 결과 cross-review workflow, framework-pure pointer, manifest contract에 half-implemented 기능은 발견하지 못했다. `run-harness-checks.sh --all`과 `check-onboarding-flows.sh`도 독립 재현 PASS했다. 다만 release note의 adopter 호환성 누락과 release Work lifecycle이 Public Clean Baseline gate와 충돌하므로 아래 must를 닫기 전에는 develop→main release event를 열 수 없다.

| ID | Severity | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| R1-Codex-F1 | must | **호환성 주의 3항목만으로는 기존 adopter 영향이 완전하지 않고, "기존 adopter 무조치"는 manifest에만 한정해야 한다.** DR-043 변경은 기존 adopter가 product constants·project verification 명령을 `AGENT-WORKFLOW.md`에서 `PLAN-SUMMARY.md`로 옮기는 one-time migration을 필수로 한다. 또한 `/cross-review`는 자동 orchestration이 아닌 manual relay라는 제품 경계가 초안에 없다. | DR-043 Decision #4는 overwrite 금지, 분류 gate와 값 보존 확인을 거친 one-time migration을 명시한다. 반면 Done Criteria와 release note는 python3/hash_mode/provenance만 호환성으로 들며 "기존 adopter 무조치"를 포괄적으로 읽히게 한다. `CROSS-REVIEW-MANUAL.md`는 전달·응답 수집이 사용자 책임임을 명시한다. | 호환성에 4번째 항목으로 **framework-pure one-time migration**을 추가하고 playbook을 pointer한다. "조치 불요"는 legacy `hash_mode`/provenance에만 한정한다. `/cross-review` 기능 설명에는 "manual relay이며 agent 간 자동 전달은 하지 않음"을 1문장 추가한다. 비목표의 "breaking change 없음"도 "신규 adopter 기준 incompatible/MAJOR contract 없음"으로 좁힌다. |
| R1-Codex-F2 | must | **gh release body 초안이 repository의 release note 계약을 충족하지 않고, 의미 있는 adopter-visible delta 하나를 누락한다.** | `VERSIONING.md` §5는 한 줄 요약, 실제 최종 명령의 fenced `검증` 섹션, 결과 요약, 전체 변경 compare link를 요구하지만 초안에는 모두 없다. tag 이후 `ee212e3`은 신규 scaffold의 Product backlog에서 `Done` 섹션을 제거하고 live-candidate-only 구조로 바꿨으나 개선 항목에 없다. | 사용자 효익 한 줄 요약을 추가하고, 실제 최종 gate가 끝난 뒤 실행 명령·결과를 `검증`에 기록하며 `v1.4.0...v1.5.0` 전체 변경 링크를 넣는다. 개선 항목에 Product backlog live-only 정리를 1줄 추가한다. final gate 전에는 "사전 검증 PASS / final gate pending"으로 단계 라벨을 분리해 완료를 과장하지 않는다. |
| R1-Codex-F3 | must | **현재 Work scope는 Public Clean Baseline gate와 lifecycle deadlock을 만든다.** 이 Work는 release 뒤에만 가능한 3-repo fleet upgrade까지 Done Criteria로 묶었으므로 release 전에 Done/Archived로 닫을 수 없지만, §3-1은 main에 internal Active Work가 남는 것을 금지한다. 현재 RC에는 CHORE-004 `status: Active`, STATUS Active pointer 공백, Done-but-unarchived CHORE-001~003도 함께 존재한다. | `docs/GIT-WORKFLOW.md` §3-1: Active Work leakage 0, Done archive-pending 0. 현재 `rg '^status: (Active|Done)$' docs/works`는 CHORE-004 Active와 CHORE-001~003 Done을 반환한다. CP1의 "sweep 완료"는 deterministic/onboarding evidence에는 맞지만 final state gate에는 맞지 않는다. | **release Work와 post-release fleet upgrade Work를 분리**한다. CHORE-004는 release readiness/event까지만 닫아 archive하고, spring·ai-deck·rfx upgrade는 tag 생성 후 수행할 follow-up Work로 넘긴다. CHORE-001~003도 archive한 뒤 clean working tree에서 §3-1 + judgment Layer를 재실행·기록한다. CP1 제목/본문은 그 전까지 "deterministic 사전 검증 PASS, final gate pending"으로 정정한다. |

**Semver Judgment:** **1.5.0 MINOR는 적정**하다. 신규 `/cross-review` workflow가 명백한 additive consumer contract이고, python3는 1.4.0 README에서도 이미 scaffold/validation 전제로 고지됐다. `--check`의 fail-closed hard requirement와 DR-043 one-time migration은 기존 adopter 호환성 주의가 필요하지만, 자동 upgrade contract가 아직 정식화되지 않은 현행 DR-028 1.x 정책에서는 MAJOR 사유가 아니다.

**Verified / Residual:** `bash scripts/tests/run-harness-checks.sh --all` PASS, `bash scripts/tests/check-onboarding-flows.sh` PASS. 실제 default scaffold에서 cross-review canonical, Claude/Codex adapter, Cursor pointer, user manual/index 존재를 확인했다. Tier 2 optional pack의 기존 DR-025 report-only dangling은 release-block으로 분류하지 않는다. 위 세 must가 닫히면 content 재검토 없이 final gate evidence만 확인해 release-go로 전환 가능하다.

### R1 — Driver Response (Claude)

전 finding 수용(defend 없음):

| Finding | 반영 |
| --- | --- |
| F1 (must) | release note 호환성에 **④ DR-043 one-time migration**(playbook pointer, overwrite 금지·값 보존) 추가. "조치 불요"를 hash_mode/provenance에 한정. `/cross-review`에 "manual relay — agent 간 자동 전달·orchestration 없음" 명시. 비목표를 "신규 adopter 기준 incompatible/MAJOR contract 없음"으로 한정 |
| F2 (must) | release note에 한 줄 요약·`검증` fenced 섹션(final gate 후 실측 기입, 현재 "pending" 라벨)·`v1.4.0...v1.5.0` compare link·Product backlog live-only 개선 1줄 추가 |
| F3 (must) | **release Work와 fleet upgrade 분리** — scope/Done Criteria에서 fleet 제거, backlog W6에 "Fleet upgrade to 1.5.0"(P1, 1:1 details, toolstead canary 보류) 등록. CHORE-001~003 archive 완료(frontmatter Archived + git mv + 양측 index). CHORE-004는 release-go 승인 시 Done+Archive 최종 commit 후 develop merge — §3-1 final gate는 그 clean 상태에서 재실행해 release PR body에 기록 |

### R1b — Consensus Confirmation Request (Cross-Agent Relay Packet)

**Role:** Driver = Claude / Reviewer = Codex / Arbiter = User

**Target:** R1 반영본 — 이 Work 파일(scope·Done Criteria·release note CP1), backlog fleet row+details, works archive 상태(-001~-003 Archived, live Active=-004뿐).

**Delta Since R1:** F1~F3 전부 반영(위 표). 잔여 절차: R1b consensus → 사용자 release-go → **-004 Done+Archive 최종 commit** → develop merge → §3-1 final gate(clean 상태) 실행 → develop→main PR(evidence 포함) → tag·gh release(`검증` 실측 기입)·sync.

**Review Objective:** F1~F3 교정 충실성 + 잔여 절차의 §3-1 정합. 신규 쟁점 없으면 **release-go consensus**(final gate evidence는 release PR에서 확인).

**Output Contract:** Verdict를 이 파일 `### R1b — Reviewer Findings (Codex)`에 기록 + Round Log 갱신 + 3줄 요약 별도 출력.

### Round Log / Consensus Log

| Round | Date | Driver | Reviewer | Verdict | 기록 |
| --- | --- | --- | --- | --- | --- |
| R1 | 2026-07-13 | Claude (prep 완료, packet 발행) | Codex (release readiness review) | conditional | half-implemented 0·MINOR 적정·deterministic/onboarding PASS; release note 호환성/형식 + release/fleet Work lifecycle 분리 must |
| R1-resp | 2026-07-13 | Claude (F1~F3 전부 수용·반영) | — | — | note 4항목 호환성·형식 보완, fleet 분리+backlog 등록, -001~-003 archive |
| R1b | 2026-07-13 | Claude (packet 발행) | Codex (대기) | — | 교정 확인 + release-go consensus 요청 |

## Checkpoints

### CP1 — Release prep + sweep 완료 (2026-07-13)

**Sweep 결과:** `run-harness-checks.sh --all` PASS(tier0 + closure + tier2 3모드 invariants + manifest contract matrix 20 case), `check-onboarding-flows.sh` PASS(Layer J-OB·Q), secret/private-info scan clean, stale identity scan clean, Layer R(VERSION↔manifest): VERSION 1.5.0 bump 후 tier2 자기일관성 PASS로 확인.

**Release note (gh release body용 — R1-F1/F2 반영본. `검증` 섹션의 최종 결과는 final gate 후 기입):**

> ## AI Workflow Harness v1.5.0
>
> cross-agent 리뷰가 정식 workflow가 되고, manifest가 자기 출처를 증언하게 되어 adopter 버전 관리가 투명해지는 릴리즈입니다.
>
> ### 새 기능
> - **`/cross-review` workflow**: driver/reviewer/arbiter 3역할 cross-agent 리뷰 절차가 정식 command로 추가 — canonical skill + Claude/Codex adapter + 사용자 매뉴얼(`docs/user/CROSS-REVIEW-MANUAL.md`). **manual relay 방식으로, agent 간 자동 전달·orchestration은 하지 않습니다**(전달·응답 수집은 사용자 책임)
> - **Manifest provenance**: scaffold가 `source_ref`/`source_commit`/`source_dirty`를 기록하고, `--check`가 version-skew(같은 버전·다른 source commit)를 감지·경고
> - **`--check` parser 강화**: manifest JSON layout 무관(pretty-print/compact 모두 정상 판정), invalid manifest는 명시적 실패(fail closed)
>
> ### 개선
> - `docs/AGENT-WORKFLOW.md` framework-pure 전환(DR-043) — product 값은 `docs/PLAN-SUMMARY.md` Implementation Baseline이 소유
> - manifest field 계약 문서화(`generated_at`=rebaseline 날짜, `hash_mode` canonical 값)
> - 신규 scaffold의 Product backlog가 live-candidate-only 구조로 정리(Done 섹션 제거)
> - 검증 스파인에 manifest contract behavior matrix(20 case) + invariant delegation 회귀 추가
>
> ### 호환성 주의 (기존 adopter)
> - **DR-043 one-time migration(필수)**: 기존 adopter는 `docs/AGENT-WORKFLOW.md`의 product constants·project verification 명령을 `docs/PLAN-SUMMARY.md`(Implementation Baseline / Verification Defaults)로 옮기는 one-time migration이 필요합니다 — overwrite 금지, 값 보존 확인 필수. 절차: `docs/maintainer/ADOPTER-UPGRADE-MIGRATION-PLAYBOOK.md` 참조
> - `--check`는 **python3 필수**가 됐습니다(부재 시 명시적 실패 — 조용한 오판 제거). scaffold 생성 경로는 python3 비의존 유지
> - `hash_mode` canonical 값이 `source_template_raw`로 변경 — 기존 adopter manifest(legacy 값)는 계속 허용되며 **이 항목과 provenance에 한해 조치 불요**(다음 upgrade/rebaseline 시 자연 전환·획득)
>
> ### 검증
> *(final gate 후 실제 실행 명령·결과 기입 — 현재: deterministic 사전 검증 PASS / final gate pending)*
>
> **전체 변경:** https://github.com/kyungseo/ai-workflow-harness/compare/ai-workflow-v1.4.0...ai-workflow-v1.5.0
