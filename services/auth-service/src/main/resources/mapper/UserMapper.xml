<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- #{} 파라미터 바인딩 사용. ${} 절대 사용 금지 (SQL Injection 방어) -->
<mapper namespace="io.kyungseo.msa.auth.mapper.UserMapper">

    <resultMap id="userResultMap" type="io.kyungseo.msa.auth.domain.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="role" column="role"/>
        <result property="enabled" column="enabled"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByUsername" parameterType="string" resultMap="userResultMap">
        SELECT id, username, email, password, role, enabled, created_at, updated_at
        FROM users
        WHERE username = #{username}
          AND enabled = TRUE
    </select>

    <select id="findById" parameterType="long" resultMap="userResultMap">
        SELECT id, username, email, password, role, enabled, created_at, updated_at
        FROM users
        WHERE id = #{id}
          AND enabled = TRUE
    </select>

</mapper>
