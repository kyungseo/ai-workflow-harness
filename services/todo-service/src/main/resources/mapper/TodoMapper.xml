<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- #{} 파라미터 바인딩 사용. ${} 절대 사용 금지 (SQL Injection 방어) -->
<mapper namespace="io.kyungseo.msa.todo.mapper.TodoMapper">

    <resultMap id="todoResultMap" type="io.kyungseo.msa.todo.domain.Todo">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="completed" column="completed"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAllByUserId" resultMap="todoResultMap">
        SELECT id, user_id, title, description, completed, created_at, updated_at
        FROM todos
        WHERE user_id = #{userId}
        <choose>
            <when test="completed != null">
                AND completed = #{completed}
            </when>
        </choose>
        ORDER BY created_at DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*)
        FROM todos
        WHERE user_id = #{userId}
        <choose>
            <when test="completed != null">
                AND completed = #{completed}
            </when>
        </choose>
    </select>

    <select id="findById" resultMap="todoResultMap">
        SELECT id, user_id, title, description, completed, created_at, updated_at
        FROM todos
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO todos (user_id, title, description, completed)
        VALUES (#{userId}, #{title}, #{description}, #{completed})
    </insert>

    <update id="update">
        UPDATE todos
        <set>
            title = #{title},
            description = #{description},
            completed = #{completed}
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateCompleted">
        UPDATE todos
        SET completed = #{completed}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM todos
        WHERE id = #{id}
    </delete>

</mapper>
